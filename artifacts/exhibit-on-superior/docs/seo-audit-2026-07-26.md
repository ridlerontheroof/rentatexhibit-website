# Holistic SEO/AEO Audit — July 26, 2026 (pre-ads launch)

Audit of www.rentatexhibit.com (production, read-only) and the local prerender
build, run with the squirrelscan CLI (v0.0.80), the repo's own validators
(JSON-LD, LCP preload, fold checks, unit-page prerender guards), and manual
review across every page type. All in-scope findings were fixed in this pass;
everything else is explicitly deferred below with a reason.

## Baseline score (production, before this pass)

squirrelscan overall: **53 / F** — Core SEO 98, Links 90, Structured Data 79,
Images 56, Content 52, Crawlability 51, Agent Experience 50, Security 44,
Performance 39.

The low overall is dominated by platform-served concerns (no HTML compression,
cache headers, security headers — see Deferred) and by a batch of fixable
on-page items, which this pass addressed. **Production will not reflect the
fixes until the next publish**; re-run `squirrel audit` after deploying.

## Post-publish score (production, after this pass)

Re-audited July 26, 2026 after the publish shipped the fixes (squirrelscan
v0.0.79, `-C full --refresh`, 108 pages crawled).

squirrelscan overall: **58 / F** (was 53) — Structured Data 79→88,
Images 56→76, Accessibility 83, Links 80, Core SEO 79, Analytics/Local
SEO/Mobile/Social/URL Structure 100. Overall remains F because the
platform-served categories are unchanged and unfixable from the artifact
(Performance 40, Security 46, Crawlability 48, Agent Experience 50 — no HTML
compression, no cache max-age, no CSP, trailing-slash 301s; all documented in
Deferred).

Post-publish findings triaged:

- **/available-units a11y errors** (search inputs uncredited wrapping label;
  slider thumbs missing `aria-valuenow` in prerendered HTML — Radix only
  applies it after hydration): fixed in this repo (explicit `aria-label` on
  the search inputs in `FloorPlans.tsx`; `ui/slider.tsx` now mirrors
  `aria-valuenow/min/max` into SSR markup). Ships with the next publish.
- **Knowledge titles 80–90 chars** (52 pages): already ticketed as the
  page-title truncation task.
- **`/sitemaps.xml` "unknown sitemap format" error**: crawler probe of a
  conventional path hitting the SPA soft-404 fallback (200 + HTML). Same
  root cause as the deferred soft-404 item; robots.txt correctly references
  only `/sitemap.xml`, which validates.
- **Duplicate titles on `?ada=1` / `?unit=` variants**: canonicals point to
  the base pages; expected, no action.
- **Remaining warnings** (VideoObject schema on unit tour videos, generic
  "view details" links on /schedule-a-tour, 4 knowledge orphan pages,
  hero images without `fetchpriority` on secondary pages): ticketed as
  follow-up tasks.

## Post-Phase-1 score (production, Express serving layer live)

Re-audited July 26, 2026 after the publish that switched serving to
`server/index.mjs` (squirrelscan v0.0.79, `-C full --refresh`, 108 pages).

squirrelscan overall: **69 / D** (was 58) — Crawlability 48→94,
Core SEO 79→95, Links 80→85, Performance 40→52, Security 46→48,
Accessibility 83→84; Analytics/i18n/Site Integrity/Legal/Local SEO/Mobile/
Social/URL Structure all 100.

Live spot-checks all confirmed: brotli on HTML, `max-age=31536000, immutable`
on `/assets/*`, 301 `/amenities/` → `/amenities`, real 404 on `/sitemaps.xml`,
HSTS/XFO/nosniff/Referrer-Policy/Permissions-Policy present.

Why Performance and Security moved less than estimated, and what was done:

- **`security/csp` still warns**: the scanner does not count
  `Content-Security-Policy-Report-Only` as a CSP. Verified zero report-only
  violations in a headless-browser pass over 12 representative pages
  (home, units, virtual tour, gallery, map, tour form, reviews, knowledge) —
  except **Google Maps on /map-directions**, whose JS loads Google fonts and a
  `mapsresources-pa.googleapis.com` endpoint at runtime. The CSP allowlist was
  widened accordingly (`style-src` + fonts.googleapis.com, `font-src` +
  fonts.gstatic.com, `connect-src` + mapsresources-pa.googleapis.com) and
  **`CSP_ENFORCE=1` is now set in the production deployment env** — the next
  publish ships the enforced header and clears this warning.
- **`perf/bad-caching` still failed**: all 108 pages lacked an
  ETag/Last-Modified validator. `server/index.mjs` now emits a weak ETag +
  Last-Modified and answers If-None-Match/If-Modified-Since with 304
  (verified locally). Clears on the next publish.
- **`security/cookie-flags` / `third-party-cookies`**: the `GAESA` cookie is
  set by the platform's Google Frontend edge, not by the app — not fixable
  here. (The edge also rewrites `Cache-Control: public` to `private`;
  max-age/immutable survive, so browser caching is unaffected.)
- **Remaining Performance warnings** (322 KB main JS chunk, ~12.9 MB tracked
  weight, LCP preload/fetchpriority on secondary pages, lazy-above-fold
  gallery cards) are exactly SEO Phase 2, already in progress as its own task.
- **Content 40, E-E-A-T 77, Agent Experience 50, Video 51**: Phases 3–5
  (existing tasks).

**After the next publish** (enforced CSP + 304 validators), re-run
`squirrel audit https://www.rentatexhibit.com -C full --refresh` — Security
should clear the CSP warning and Performance the bad-caching failure.

## Post-publish confirmation (enforced CSP + 304 validators live)

Re-audited July 26, 2026 after the publish that enabled `CSP_ENFORCE=1` and the
ETag/Last-Modified validators (squirrelscan v0.0.79, `-C full --refresh`,
108 pages).

squirrelscan overall: **71 / C** (was 69/D) — Security 48→49,
Performance 52→55, Crawlability 94, Core SEO 92, Accessibility 84→85,
Structured Data 88, Links 94; Video 51→100 (VideoObject metadata shipped
alongside this publish).

Both targets of this pass confirmed live:

- **`security/csp` failure cleared**: `curl -sI https://www.rentatexhibit.com/`
  shows a real `Content-Security-Policy` header (not Report-Only). The only
  remaining CSP note is a warning — `csp-unsafe-scripts: CSP allows
  'unsafe-inline'` — which is intentional (required by the GTM bootstrap +
  availability-prefetch inline scripts in the prerendered head); removing it
  would need nonces/hashes wired through the prerender, a separate project.
- **`perf/bad-caching` failure cleared** (absent from the report entirely):
  the home page serves `ETag: W/"…"` + `Last-Modified`, and a conditional
  request with `If-None-Match` returns **304** — verified live with curl.
- **Zero CSP violations under enforcement**: headless-browser pass over 10
  live pages (home, /map-directions, /virtual-tour, /photo-gallery, /reviews,
  /available-units, /schedule-a-tour, /amenities, /knowledge, /contact-us)
  with console + `securitypolicyviolation` listeners — 0 violations,
  including Google Maps fonts/endpoints on /map-directions.

Remaining errors are all known/ticketed elsewhere: 5 a11y errors (unlabeled
search inputs on /available-units — fix already in repo awaiting publish;
label-content-name mismatch on /faq "full answer" links — covered by existing
a11y guard tasks), 2 image errors, and `perf/total-byte-weight` (~10.7 MB
tracked — SEO Phase 2). Security's remaining warnings are the platform GAESA
cookie flags and third-party GTM cookie (not fixable in app) plus form
CAPTCHA (product decision).

## Post-phases-1–4 full audit + Phase 5 cleanup (2026-07-26, latest)

Re-audited after the publish that shipped Phases 2–4 (build ms26q9jg,
squirrelscan v0.0.79, `-C full --refresh`, 109 pages).

squirrelscan overall: **72 / C** (was 71) — Content 41, Agent Experience 50,
Performance 55, Security 49 remain the low categories; E-E-A-T 77→**100**,
Video **100**, Links 94, Crawlability 94, Core SEO 92, Accessibility 85,
Images 82, Structured Data 88; nine categories at 100.

Phase 5 fixes applied in this pass (in repo, take effect next publish):

- **Contrast token**: `--muted-foreground` darkened 45%→40% lightness —
  ≥4.5:1 on both white and the muted (96%) background; visually checked on
  home/available-units (no design regression). Clears the 34
  `a11y/color-contrast` warnings.
- **Param-variant duplicate titles/descriptions**: `?ada=1` (available
  units), `?unit=` (schedule-a-tour, schedule-showing) now set distinct
  client-side titles + descriptions; canonicals unchanged. Verified rendered
  titles in a headless browser.
- **Heading order on /schedule-showing**: always-present `sr-only` H2 so the
  prerendered/loading state no longer skips H1→footer-H3 (was flagged on all
  8 unit variants).
- **AGENTS.md** added at the site root (ax/agents-md notice).
- schema/video warnings on newer units: the YouTube metadata cache already
  covers every current video — the flags measured the pre-publish build and
  clear on republish.

Triaged, not fixed here (structural / already ticketed / scanner noise):

- `ax/token-weight` (105 pages <15% visible text): inherent SPA markup ratio;
  the `.md` twins + llms.txt are the agent-side answer. Raising the HTML
  text ratio further means SSR-only rendering — separate project.
- `perf/total-byte-weight` ~10.8 MB tracked: photography-heavy; all images
  are already ≤200 KB with AVIF/WebP rungs and lazy-loading.
- `perf/lcp-fetchpriority` / `lcp-hints`: heroes ARE eager+`fetchpriority`
  with AVIF `imagesrcset` preloads — the scanner doesn't associate the AVIF
  preload with the rendered WebP `src` (no `href` on `imagesrcset` preloads).
- `perf/lazy-above-fold` on AppFolio CDN collage photos: first photo is
  eager; the remainder sit mostly below the fold on common viewports.
- Thin knowledge articles (<300 words on ~70 `/knowledge/*` pages) +
  keyword-density notices: content program, proposed as follow-up work.
- /faq label mismatch + /available-units input labels: fixes already merged
  via other tasks, awaiting the same publish.
- GAESA cookie flags, GTM third-party cookie, form CAPTCHA, Instagram 429:
  unchanged platform/product items.

**After the next publish**, re-run
`squirrel audit https://www.rentatexhibit.com -C full --refresh` and record
the final score here. Expected movement: Accessibility 85→~95 (contrast +
label fixes), Core SEO 92→~95+ (duplicate titles cleared), Content/E-E-A-T
duplicates cleared; Content and Agent Experience remain capped by the
structural items above until the knowledge-article deepening happens.

## What was verified healthy (no action needed)

- robots.txt welcomes all major crawlers incl. AI engines; references sitemap.
- sitemap.xml complete: 97 URLs — static pages, all 8 published unit pages,
  all knowledge articles; matches prerendered routes and artifact rewrites.
- Canonicals consistent (all `https://www.rentatexhibit.com`, no apex mix).
- Apex → www redirect live (Squarespace forwarding). HTTP 301 confirmed.
- llms.txt and llms-full.txt fresh (regenerated each build; 97 pages).
- IndexNow key file served correctly at `/fc45d4f042a0d056c865fb0b348a0065.txt`;
  api-server pings `api.indexnow.org` (covers Bing) on availability changes and
  submits sitemap key URLs once per publish.
- Structured data: Core SEO / JSON-LD graph valid on every page type
  (Apartment/OfferForLease with priceValidUntil, FloorPlan, ApartmentComplex +
  LocalBusiness split, FAQPage, BreadcrumbList, ADA amenityFeature). Build-time
  JSON-LD validator passes across all 97 pages.
- Unit-page noindex behavior for rented units, prerender guards, knowledge
  sitemap guard — all pass in build.
- H1s and titles unique/intent-matched on all page types; knowledge pages open
  answer-first; internal-link graph reaches money pages from every article.
- Fold checks pass (unit card above the fold on short-laptop and phone).

## Fixed in this pass

| Finding | Fix |
| --- | --- |
| Unit-page meta descriptions ~260 chars (truncated in SERPs/ads) | `unitDescription()` now emits the first fact-dense sentence clamped ≤160 chars (`src/data/unitPageSeo.ts`); full summary remains in body + JSON-LD |
| /knowledge hub description ~197 chars | Shortened to ~155 (`src/data/seo.ts`) |
| /reviews LocalBusiness JSON-LD missing address/telephone when nodes aren't merged by @id | Address + telephone added directly to the reviews node (`src/data/reviews.ts`) |
| Unit-card link `aria-label` mismatched visible text (WCAG 2.5.3) | aria-label now starts with the visible "Apt NNNN details" (`AvailableUnits.tsx`) |
| Sort dropdown: Radix Select's hidden native clone flagged (focusable in aria-hidden, unlabeled) | Replaced with a styled native `<select>` with sr-only label (`FloorPlans.tsx`) — also works pre-hydration |
| Range-slider thumbs without accessible names | `Slider` accepts `thumbLabels`; sqft slider thumbs labeled Min/Max (`ui/slider.tsx`, `PlanFilters.tsx`) |
| Unit-page lead photo lazy-loaded above the fold (LCP delay) | First collage photo now eager + `fetchPriority="high"`, wrapped in `<picture>` to stay compatible with the React 19 preload guard (`UnitDetail.tsx`) |
| YouTube iframe on unit pages without width/height (CLS) | Explicit 1280×720 attributes added (`UnitDetail.tsx`) |
| Heading skip H1→H3 on /virtual-tour | Section headings promoted to H2 (styling unchanged) |
| Thin content (<300 words) on /contact-us, /photo-gallery, /residents, /reviews, /virtual-tour | One crawlable, fact-only section added per page with contextual internal links to /available-units, /schedule-a-tour, /knowledge |

Full test suite (762 tests), prerender build with all guards, and fold checks
pass after these changes. Availability snapshot drift reverted.

## Update (2026-07-26, later): serving-layer deferrals fixed in repo

SEO Phase 1 (see `seo-score-plan-2026-07.md`) replaced static publish with an
Express server (`server/index.mjs`, autoscale): Brotli/gzip pre-compression,
immutable asset caching + short HTML caching, HSTS/XFO/nosniff/Referrer/
Permissions-Policy headers, CSP in Report-Only, non-slash 301s (redirect
direction flipped to match canonicals), and real 404s (including the
`/sitemaps.xml` probe). The first four "Deferred" items below are therefore
resolved in the repo and take effect on the next publish; re-run
`squirrel audit https://www.rentatexhibit.com -C full --refresh` afterwards
and record the new scores here.

## Deferred (with reasons)

- **Trailing-slash 301s** (`/amenities` → `/amenities/` etc.): the static host
  applies a directory-index redirect before rewrites, even though the artifact
  config declares explicit non-slash rewrite pairs for every route. Single-hop
  301 with consistent non-slash canonicals — Google consolidates correctly.
  Not fixable at code level without flipping the entire canonical scheme to
  trailing-slash (high-risk churn across canonicals, sitemap, guards, tests).
  If ad landing URLs are entered with a trailing slash (e.g.
  `https://www.rentatexhibit.com/schedule-a-tour/`), the redirect is avoided
  entirely — recommended for the Ads campaign.
- **Soft 404s** (unknown paths return 200 with the SPA fallback): inherent to
  static SPA hosting; the fallback carries the home canonical so unknown URLs
  consolidate to the homepage rather than index-bloating. `/knowledge/*` has a
  dedicated noindex not-found stub. Low risk; revisit only if GSC reports
  soft-404 growth.
- **No HTML compression, `cache-control: private`, no CSP/X-Frame-Options**:
  headers are set by the platform edge for this deployment type and are not
  configurable from the artifact's static-serve config. Platform-level; note
  for a future platform revisit. HTML payloads are small (~60–90 KB), assets
  are hashed and long-lived.
- **Floor-plan thumbnails `alt=""`**: intentional. The thumbnails sit inside
  buttons whose accessible name is the card's visible text; adding alt would
  break WCAG label-in-name. The full floor-plan images in the lightbox carry
  descriptive alt text.
- **Filter search inputs "unlabeled"**: false positive — both inputs are
  wrapped in `<label>` with an sr-only text span, which is a valid labeling
  technique.
- **Instagram link 429 in link checker**: false positive (Instagram
  rate-limits crawlers).
- **321 KB main bundle / ~13 MB total weight, 24 images >200 KB**: photography-
  heavy pages lazy-load below-fold images; LCP images are preloaded AVIF.
  Bundle held steady through the recent merges; no bloat regression found.
  A per-route bundle budget is a reasonable future guard, not a launch blocker.
- **About page (E-E-A-T)**: new content program — out of scope per task;
  recommended as future content work.

## Ads landing-page readiness (home, /floor-plans → /available-units, /schedule-a-tour)

- All four prerender with per-page titles/descriptions/OG; LCP preloads intact;
  CLS guards pass; mobile fold checks pass.
- Clear conversion paths: persistent Schedule a Tour CTAs, tap-to-call phone
  number, tour form with server-side lead handling + AppFolio guest-card push.
- Message match: page H1s name River North / luxury apartments / availability —
  align ad copy with those phrases for Quality Score.
- Recommendation: use trailing-slash final URLs in Ads (see above) to skip the
  301 hop the platform adds.

## Owner action items

1. **After the next publish**, re-run the audit (`squirrel audit
   https://www.rentatexhibit.com`) to confirm the on-page score lift.
2. ~~**Squarespace forwarding**: `https://rentatexhibit.com` currently 301s to
   `http://www.rentatexhibit.com` (http, not https), adding an extra hop. If
   Squarespace's forwarding UI allows, set the destination to
   `https://www.rentatexhibit.com`.~~ **Done (2026-07-26)** — forwarding
   destination updated to `https://www.rentatexhibit.com`. **Verified after
   propagation (2026-07-26):** `curl -sIL https://rentatexhibit.com` shows a
   single 301 hop with `location: https://www.rentatexhibit.com` followed by
   the 200 — no http:// intermediate.
3. **Google Ads**: enter final URLs with trailing slashes; keep ad copy matched
   to page H1s.
4. **Search Console**: after publish, request re-indexing of the home page and
   the four landing pages; watch the soft-404 and review-snippet reports.
5. **Leasing-team facts** for the breed-restriction / short-term-lease answers
   remain pending (existing task).
